[
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit",
    "where": "landing facts, README",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo, activate service worker, set offline, reload, and use sample markers"
  },
  {
    "id": "demo-isolation",
    "claim": "Demo sample data never reads or writes real markers",
    "where": "Demo banner, README",
    "test": "npm run test:e2e -- --grep @claim:demo-isolation",
    "sandbox": "create a real marker, enter /demo, create/reset a demo marker, then return to real mode"
  },
  {
    "id": "local-only",
    "claim": "Markers stay in this browser",
    "where": "landing facts, privacy page",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "demo capture flow records every request and asserts only the same origin is used"
  },
  {
    "id": "reference-only-manual-timer",
    "claim": "Links are saved as references and the timer is manual",
    "where": "link source note, README",
    "test": "npm run test:e2e -- --grep @claim:reference-only-manual-timer",
    "sandbox": "demo link source; assert the saved link and an explicit 15-second manual timer action"
  },
  {
    "id": "local-file-no-upload",
    "claim": "Local media is not uploaded",
    "where": "local-file source note, README",
    "test": "npm run test:e2e -- --grep @claim:local-file-no-upload",
    "sandbox": "demo local WAV selection; assert an in-page audio element and only same-origin requests"
  },
  {
    "id": "no-account-or-analytics",
    "claim": "The app has no account or analytics",
    "where": "privacy page, README",
    "test": "npm run test:e2e -- --grep @claim:no-account-or-analytics",
    "sandbox": "fresh /demo visit; assert the task works with no account fields and every request is same-origin"
  },
  {
    "id": "no-payment-or-account",
    "claim": "No payment or account is required",
    "where": "landing facts",
    "test": "npm run test:e2e -- --grep @claim:no-payment-or-account",
    "sandbox": "fresh ?demo=1 visit; assert the task is available and no account or payment fields exist"
  }
]
