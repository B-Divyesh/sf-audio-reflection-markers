{
  "name": "audio-reflection-markers",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src tests vite.config.ts playwright.config.ts",
    "build": "npm run typecheck && vite build && node scripts/verify-dist.mjs && node scripts/check-budget.mjs",
    "preview": "node scripts/serve-dist.mjs",
    "test": "vitest run tests/unit",
    "test:e2e": "playwright test",
    "test:artifact": "node scripts/verify-dist.mjs",
    "test:update": "node scripts/check-update.mjs",
    "check:budget": "node scripts/check-budget.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "1.58.2",
    "@types/node": "^24.3.0",
    "eslint": "^10.9.1",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.68.0",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
